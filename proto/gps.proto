syntax = "proto2";

package gnss_driver;

import "header.proto";
import "pose.proto";

message Gps {
  optional gnss_driver.Header header = 1;

  // Localization message: from GPS or localization
  optional gnss_driver.Pose localization = 2;
}
